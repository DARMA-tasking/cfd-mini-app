name: Docker Build CI

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Docker Build
        id: docker_build
        uses: docker/build-push-action@v2
